x-icaf-comment: |
  (cac) Not sure we actually need this. Remove it?
description: |
  A summary of the names and types of policies associated with the client
properties:
  name:
    type: string
  policyId:
    type: string
  lineOfBusiness:
    enum: ["auto", "homeowners"]

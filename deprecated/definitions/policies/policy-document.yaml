  properties:
    pageId:
      type: string
    createdDate:
      type: string
      format: date
    documentType:
      type: string
    deliveryMethod:
      description: Such as email, postal, etc
      type: string
  required: ["pageId", "documentType"]

description:  Supplemental documentation for a policy.
required:
  - 'code'
properties:
  code:
    description: |
      The code assigned to the document by the record-keeping system.
    type: string
  description:
    type: string
  dateReceived:
    type: string
    format: date
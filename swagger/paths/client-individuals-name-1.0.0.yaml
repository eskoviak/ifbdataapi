swagger: "2.0"
info:
  version: 1.0.0
  title: client-individuals-name
  x-architectural-layer: system
  description: |
    This is a system level API that updates the client name for individuals

    #### Service Functionality
    This API is called with a {clientId} parameter. If successful, the resultant is a HTTP 200 status code.

    #### Diagrams
    TBD

    #### Performance Expectation
    Standard SLA applies.

    #### Service Capacity Supported
    Standard Capacity applies

    #### Suggested Test Scenario
    TBD

    #### Service Availability
    Standard Service Availability applies

    #### Service Security
    HTTPS/SSL

  contact:
    name:  Mary Boscaccia
    url:  http://www.infarmbureau.com
    email: mary.Boscacci@infarmbureau.com
produces:
  - application/json
schemes:
  - http
  - https
basePath:
  /api/clients/individuals
paths:
  /{clientId}/name:
    'post':
      summary: Corrects client name
      description: For a given clientId, corrects the client name for individual client type.
      parameters:
          - $ref: '#/parameters/ClientId'
          - name: personName
            description: The desired person name information for this operation
            required: true
            in: body
            schema:
              $ref: ../definitions.yaml#/definitions/PersonName
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized or invalid token
        '404':
          description: Entity not found
        '500':
          description: Server error
    'put':
      summary: Changes client name
      description: For a given clientId, changes the client name for individual client type.
      parameters:
        - $ref: '#/parameters/ClientId'
        - name: personName
          description: The desired person name information for this operation
          required: true
          in: body
          schema:
            $ref: ../definitions.yaml#/definitions/PersonName
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized or invalid token
        '404':
          description: Entity not found
        '500':
          description: Server error
parameters:
  ClientId:
    description: The identifier of the client
    name: clientId
    required: true
    in: path
    type: string